name: Update Dojo

on: push

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: pwncollege/dojo-update@v1
        with:
          dojo: cse543-2025-spring
          update_code: ${{ secrets.UPDATE_CODE }}
